@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">Movimiento {{ $movimiento->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('/movimiento') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/movimiento/' . $movimiento->id . '/edit') }}" title="Edit Movimiento"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('movimiento' . '/' . $movimiento->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-xs" title="Delete Movimiento" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $movimiento->id }}</td>
                                    </tr>
                                    <tr><th> Fecha </th><td> {{ $movimiento->fecha }} </td></tr><tr><th> Monto </th><td> {{ $movimiento->monto }} </td></tr><tr><th> Tipo </th><td> {{ $movimiento->tipo }} </td></tr><tr><th> Balance Id </th><td> {{ $movimiento->balance_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
